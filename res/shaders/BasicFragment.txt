#version 330 core  
out vec4 FragColor;

in vec4 ourColor;
in vec3 Normal;

uniform vec3 u_LightColor;
uniform vec3 u_lightPos;

void main()
{
     float alpha = ourColor.a;
     FragColor = vec4(u_LightColor * ourColor, alpha);
};