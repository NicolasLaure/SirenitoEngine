#version 330 core  
out vec4 FragColor;

in vec4 ourColor;
in vec2 ourTextureCoordinates;

uniform vec4 u_LightColor;
uniform sampler2D u_Texture;

void main()
{
    	vec4 tintedColor = texture(u_Texture,  ourTextureCoordinates) * ourColor;
	FragColor = u_LightColor * tintedColor;
};